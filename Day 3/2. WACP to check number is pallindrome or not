#include<stdio.h>
int main()
{
int n,r,rev=0;
printf("Enter any num ");
scanf("%d",&n);
while(n!=0)
{
	r=n%10;
	rev= rev*10+r;
	n=n/10;
}
if(n==rev)
   {
	printf("the num is a pallindrome");
   }
   else
    {
	printf("the num is not a pallindrome");
   }
   return 0;
}
